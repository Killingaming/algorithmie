#DEBUT
#On définit une fontion ticTacToe qui seras la principale, celle du jeu :
    def __init__(self):
        self.board = []
    #On définit une "gameBoard"  (plan de jeu):
    def create_board():
        for i in range(3):
            row = []
            for j in range(3):
                row.append('_')
            board.append(row)
    for 
        #On définit un tableau A de len X 
            #Pour X = 3 -> tabA = [0,1,2]
        #On définit un tableau B de len y
            #Pour Y = 3 -> tabB = [0,1,2]
    #On définit une fonction case qui auras comme paramètre X et Y
    #Tant que 
    #Joueur 1 est en train de jouer 
        #Demander au joueur 1 de choisir une ligne X
        #Demander au joueur 1 de choisir une ligne Y
        #print(X)
    #Tant que 
    #Joueur 2 est en train de jouer 
        #Demander au joueur 2 de choisir une ligne X
        #Demander au joueur 2 de choisir une ligne Y
        #print(Y)

    #Tant que 
     
    
    
    #print("{player is the winner")

#FIN

"REMEMBER, only the CPU can win or draw, the player 1 (the real) CANNOT win."